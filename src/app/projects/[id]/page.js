"use client";

const ProjectDetail = ({ params }) => {
  return (
    <div
      className={`w-full h-full flex bg-[#ADD8E6] pb-[30px] justify-center items-center pt-[50px]`}
    >
      <div className="flex items-center gap-[50px] flex-col"></div>
    </div>
  );
};

export default ProjectDetail;
